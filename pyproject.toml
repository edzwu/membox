[project]
name = "membox"
version = "0.1.0"
description = "Membox v0.1: nvim-as-client + Python core (DDD-ish) + JSON envelope protocol"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
memboxd = "membox.apps.memboxd:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
